import type { AgentRole } from "../workflow/types.ts";
export function memoryQuery(role: AgentRole, summary: string): string { const subjects = { planner: "Architect architecture conventions", implementation: "Smith implementation conventions and previous decisions", security: "Sentinel security invariants and threat model", review: "Inquisitor review conventions and architectural constraints" }; return `${subjects[role]} relevant to: ${summary}`; }
